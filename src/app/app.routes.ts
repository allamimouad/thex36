import { Routes } from '@angular/router';
import { TabsFeaturesComponent } from './features/tabs-features/tabs-features.component';

export const routes: Routes = [
  {
    path: '',
    component: TabsFeaturesComponent,
  },
];
